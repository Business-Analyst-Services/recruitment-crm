{
  "name": "talentledger-crm",
  "version": "0.1.0",
  "private": true,
  "description": "TalentLedger — a lightweight CRM for recruitment agencies.",
  "engines": {
    "node": ">=22.13"
  },
  "scripts": {
    "predev": "node --no-warnings scripts/seed.mjs",
    "dev": "next dev",
    "prestart": "node --no-warnings scripts/seed.mjs",
    "build": "next build",
    "start": "next start",
    "seed": "node --no-warnings scripts/seed.mjs",
    "reset": "node --no-warnings scripts/seed.mjs --reset",
    "typecheck": "tsc --noEmit"
  },
  "dependencies": {
    "next": "15.5.26",
    "react": "19.1.1",
    "react-dom": "19.1.1",
    "server-only": "^0.0.1"
  },
  "devDependencies": {
    "@types/node": "^22.10.0",
    "@types/react": "^19.1.0",
    "@types/react-dom": "^19.1.0",
    "typescript": "^5.6.0"
  }
}
